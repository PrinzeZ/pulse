 # H2 Local Database — Change Log This branch adds an optional local H2 cache for offline support, without changing the existing PostgreSQL/Supabase setup or any business logic/controllers. ## Changes - Added H2 dependency to pom.xml (runtime scope only) - Added application-local.properties with local.datasource.* keys for the H2 connection (separate from spring.datasource.* which still points to Supabase/PostgreSQL, untouched) 