package com.pulse;

import org.springframework.context.annotation.Configuration;

/** MVC configuration intentionally contains no security checks. Spring Security owns request authorization. */
@Configuration
public class WebConfig {
}
